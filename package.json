{
  "name": "quadra",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm --filter @quadra/web dev",
    "dev:web": "pnpm --filter @quadra/web dev",
    "dev:mobile": "pnpm --filter @quadra/mobile start",
    "build": "pnpm --filter @quadra/web build",
    "lint": "pnpm --filter @quadra/web lint"
  },
  "packageManager": "pnpm@10.33.3",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "puppeteer-core": "^25.11.0"
  }
}
